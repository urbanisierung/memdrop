{
	"name": "memdrop",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"check": "biome check .",
		"dev": "vite dev",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"preview": "vite preview",
		"deploy": "rm -f dist/wrangler.json .wrangler/deploy/config.json && wrangler pages deploy dist/ --project-name=memdrop"
	}
}
